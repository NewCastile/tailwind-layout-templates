---
export interface Props {
	raw: string
	lang: any
}

const { raw, lang } = Astro.props

import { Code } from "astro/components"
---

<Code class={"text-xs h-full p-4"} theme={"dark-plus"} code={raw} lang={lang} />
